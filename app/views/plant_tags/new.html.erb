<div class="container">
  <%= simple_form_for [@plant, @plant_tag] do |f| %>
    <%= f.association :tag, input_html: {data: {controller: "tom-select"}} %>
    <%= f.submit "Créer un plant tag", class: 'btn btn-primary' %>
  <% end %>
</div>
